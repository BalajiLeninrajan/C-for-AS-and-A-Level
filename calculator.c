#include "helpers.h"

int main(void)
{
    // Getting what the user wants to do
    char operation;
    outputns("Enter operation ( + | - | / | * | ! ): ");

    // Gets input and converts String to Char (Don't question it, it will make sense later)
    operation = input()[0];

    // Write code to so that
    // A. The user can provide the numbers they want to operate on
    // B. The operation is processed
    // C. The result is shown
    // For division on show only the integer part, maybe the insert could help you here
    // Bonus points for making the code look nice
    // Bonus points for making the output look nice
}
